=render '/partials/header_rating'
=render 'projects'
%section.mission
  =image_tag('photo2.jpg', class: 'paralax')
  .container
    .triangle
    .left
      %h3 Місія:
      %p створення нових можливостей самореалізації мешканців Івано-Франківська задля досягнення високого рівня якості життя в місті через більшу спроможність, довіру та амбітність 
    .right
      %a.button.red.support-button{href: "#step1"} підтримати
- if @static_text.present?
  = render 'main/info'

= render 'main/map'
= render 'main/news'
